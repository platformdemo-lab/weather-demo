# weather-demo

`weather-demo` is a sample Flask service used to validate the platform flow end to end.

## Purpose

- Validate the shared Jenkins pipeline integration
- Validate Backstage catalog, source, and Jenkins visibility
- Provide a simple service for local testing
